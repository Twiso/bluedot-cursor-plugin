---
name: bluedot-tasks
description: Extract tasks from a Bluedot recording and offer to create them in Linear, Jira, Trello, or another connected ticket system.
---

Follow the `meeting-to-tasks` skill (and `use-bluedot-meetings` for tool use).

Extract tasks from a recording and propose creating them in a connected ticket system. Confirm which tasks before creating. If exactly one tracker is connected, use it after that confirmation.

Meeting name, URL, or id (optional; defaults to the latest meeting you joined): $input
